# Userbot runtime. Install from the repository root:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r userbot/requirements.txt
#
websocket-client>=1.6.0
requests>=2.28.0
# Optional Firefox TLS/JA3 impersonation.  Not required on Termux — the
# known-good path is requests + websocket-client (login first, then wss
# with Cookie: ssid=…).  Install it if Cloudflare drops the handshake.
curl_cffi>=0.7.0
