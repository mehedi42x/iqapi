# Userbot runtime. Install from the repository root:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r userbot/requirements.txt
#
websocket-client>=1.6.0
requests>=2.28.0
# Firefox TLS/JA3 impersonation — required to get past Cloudflare on the
# IQ Option websocket.  Without it the handshake times out after ~20s.
curl_cffi>=0.7.0
