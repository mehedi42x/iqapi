# Runtime dependencies for the `iqoptionapi` + `bot` packages.
# Install with:  pip install -r requirements.txt   (or:  pip install .)
websocket-client>=1.6.0
requests>=2.28.0
curl_cffi>=0.7.0

# iq_option_api is bundled in this repository/distribution; it is not an
# external PyPI dependency.
